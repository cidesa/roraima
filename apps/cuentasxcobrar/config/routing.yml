# default rules
homepage:
  url:   /
  param: { module: main, action: index }

default_symfony:
  url:   /symfony/:action/*
  param: { module: main }

default_index:
  url:   /:module
  param: { action: index }

default:
  url:   /:module/:action/*
    
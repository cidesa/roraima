generator:
  class:              cidesaPropelAdminGenerator
  param:
    model_class:      Viaestado
    theme:            cidesaGenerator
    fields:
      codest:         { name: CÃ³digo Estado, params: maxlegth=4 size=5 onBlur="$(this.id).value=$(this.id).value.pad(4,'0',0)"}
      nomest:         { name: DescripciÃ³n, params: maxlegth=100 }
      codpai:         { name: CÃ³digo PaÃ­s}
      nompai:         { name: DescripciÃ³n}
      pais:          { name: .}

    list:
      title: Listado de Estados
      display: [=codest, nomest, codpai, nompai]
      filters: [codest, nomest]
      sort:    codest
      objects_actions:
        _edit:        ~
        _delete:        ~
    edit:
      title: EdiciÃ³n de Estados
      display:
        Registro Estados: [_pais, codest, nomest ]
      actions:
        _list:        ~
        _save:        ~
        _delete:      ~